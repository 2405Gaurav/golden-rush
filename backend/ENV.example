### Spring Boot / Neon Postgres
# Copy values into your shell env (PowerShell) before running:
#   $env:DATABASE_JDBC_URL="..."
#   $env:DATABASE_USERNAME="..."
#   $env:DATABASE_PASSWORD="..."

# Neon JDBC URL should NOT include user/password query params; keep them separate:
DATABASE_JDBC_URL=jdbc:postgresql://YOUR-NEON-HOST/neondb?sslmode=require&channelBinding=require
DATABASE_USERNAME=neondb_owner
DATABASE_PASSWORD=REPLACE_ME

### JWT (base64-encoded 32+ bytes)
JWT_SECRET_BASE64=REPLACE_ME

### CORS (frontend origin)
CORS_ALLOWED_ORIGINS=http://localhost:3000

### Bootstrap admin (created on startup if not exists)
BOOTSTRAP_ADMIN_ENABLED=true
BOOTSTRAP_ADMIN_EMAIL=admin@sweetshop.local
BOOTSTRAP_ADMIN_PASSWORD=admin123!

### Bootstrap sweets (created on startup if database is empty)
BOOTSTRAP_SWEETS_ENABLED=true
BOOTSTRAP_SWEETS_DEFAULT_QUANTITY=100





